#!/bin/bash
ALIAS=$1
HOST=$2
if test -z "$ALIAS" -o -z "$HOST"
then
  echo "Usage: $0 <alias> <hostname>" >&2
  echo
  echo "Example: $0 puppet prod1" >&2
  echo
  exit 1
fi
DIR=$(basename $0)
exec $DIR/add_entry "$ALIAS" "CNAME" "$HOST"
