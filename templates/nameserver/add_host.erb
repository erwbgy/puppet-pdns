#!/bin/bash
HOST=$1
IP=$2
if test -z "$HOST" -o -z "$IP"
then
  echo "Usage: $0 <hostname> <ip>" >&2
  echo
  echo "Example: $0 puppet 10.0.0.1" >&2
  echo
  exit 1
fi
DIR=$(basename $0)
exec $DIR/add_entry "$HOST" "A" "$IP"
